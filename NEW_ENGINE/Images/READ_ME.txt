This project when built will automatically comvert images placed in it into the required DDS format.

1. Put original file in to the required folder USING FILE EXPLORER, do not just drag and drop here.
2. Right click on Project Add -> Existing Item -> and then select the new image
3. Right click on the image now in the project  select Properties
4. Change the Item Type to "Image Content Pipeline"
5. Build the project, DDS versions of the images will now appear in a directory called DDS